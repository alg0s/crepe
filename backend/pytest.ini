[pytest]
markers =
    unit: fast isolated tests for pure logic
    integration: tests spanning multiple modules (pipeline/api/storage)
    regression: tests that lock down previously-fixed failure modes
